# Normalize line endings. Shell scripts must stay LF so they run inside Linux
# containers regardless of the host OS (prevents "bad interpreter" errors).
* text=auto eol=lf
*.sh text eol=lf
*.png binary
*.jpg binary
*.ico binary
