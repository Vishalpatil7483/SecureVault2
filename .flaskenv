# Non-secret Flask CLI defaults, auto-loaded by python-dotenv.
# This lets you run `flask run` / `flask db ...` without `--app run:app`.
# Safe to commit — it must never contain secrets (those go in .env).
FLASK_APP=run.py
